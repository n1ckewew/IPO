numbers=[3, 6, 8, 12, 9, 2, 5]
larg_el=0
for num in numbers:# Проверяем, что текущий элемент делится на 2 без остатка
    if num % 2==0:
        # Проверяем, является ли текущий элемент больше текущего наибольшего найденного элемента
        if num > larg_el:
            larg_el=num
print("Наибольший элемент который делится на 2 без остатка", larg_el)
